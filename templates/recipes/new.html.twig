{% extends "base.html.twig" %}

{# Page du formulaire de l'ajout d'un véhicule au showroom (formulaire) #}
{% block title %}Ajout d'une recette au garage{% endblock %}

{# {% form_theme myform _self %} #}
{# plus besoin de cette ligne car j'ai initié par défaut le style des formulaires #}
{% form_theme myform 'bootstrap_5_layout.html.twig' %}

{% block body %}

    <div class="container-fluid slide ">
        <h1 style="margin-bottom: 50px; margin-top: 30px; text-align:center;">Ajout d'une nouvelle recette</h1>
        <p style="text-align:center;"><b>Entrez les données correspondant à la recette que vous désirez ajouter</b></p>
        {{form_start(myform)}}
            <div class="container" style="background-color:  rgba(255, 170, 22, 0.1); padding: 20px; border-radius: 20px;">
                <div>
                    
                    {{form_widget(myform)}}

                </div>
                <button type="submit" class="btn btn-warning mt-3">Ajouter la recette</button>
            </div>
            
        {{form_end(myform)}}
    </div>
{% endblock %}



